//
//  TodoListAlertRouter.swift
//  TodoApp
//
//  Created by alican on 12.01.2022.
//

import Foundation
/*
final class TodoListAlertRouter : TodoListAlertRouterProtocol{

     unowned let view: UIViewController
     var service : TodoListAlertServiceProtocol!
     
     init(view: UIViewController) {
         self.view = view
     }

    func navigate(to route: TodoListAlertRoute) {
        switch route {
        case .unknown(let todo):
            service = TodoListService()
            let unknownListViewModel = TodoUnknownViewModel(service: service, todo: todo)
            let unknownView = TodoDetailBuilder.make(with: unknownListViewModel, todo: todo)
            view.show(unknownView, sender: nil)
        }
    }
}
*/
